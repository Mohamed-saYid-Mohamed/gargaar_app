class CallLogService {
  Future<void> logCall(
    String userId,
    String label,
    String number,
  ) async {
    // TODO: Replace with real API / backend call
    await Future.delayed(const Duration(milliseconds: 300));
  }
}
